{"subjectArea":"Internal - Reporting Calendar Dynamic Time Filters","pillar":"SCM","pillars":["SCM"],"nodes":[{"id":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","type":"tableNode","position":{"x":100,"y":0},"data":{"label":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","type":"Physical Table","columns":["CALENDAR_TYPE","PILLAR","TIME_LEVEL","TIME_SEQ","TIME_VALUE"],"isExtensible":false}},{"id":"Time","type":"tableNode","position":{"x":700,"y":0},"data":{"label":"Time","type":"Presentation Table","columns":["Calendar Type","Pillar","Record Count","Time Level","Time Sequence","Time Value"],"isExtensible":false}},{"id":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","type":"tableNode","position":{"x":100,"y":120},"data":{"label":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","type":"Physical Table","columns":["CALENDAR_TYPE","PILLAR","TIME_LEVEL","TIME_SEQ","TIME_VALUE"],"isExtensible":false}}],"edges":[{"id":"e-1","source":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","target":"Time","animated":true,"style":{"stroke":"rgba(255, 255, 255, 0.15)","strokeWidth":2}},{"id":"e-2","source":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","target":"Time","animated":true,"style":{"stroke":"rgba(255, 255, 255, 0.15)","strokeWidth":2}}],"mappings":[{"presentationTable":"Time","presentationColumn":"Calendar Type","physicalTable":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"CALENDAR_TYPE"},{"presentationTable":"Time","presentationColumn":"Calendar Type","physicalTable":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"CALENDAR_TYPE"},{"presentationTable":"Time","presentationColumn":"Pillar","physicalTable":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"PILLAR"},{"presentationTable":"Time","presentationColumn":"Pillar","physicalTable":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"PILLAR"},{"presentationTable":"Time","presentationColumn":"Time Level","physicalTable":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_LEVEL"},{"presentationTable":"Time","presentationColumn":"Time Level","physicalTable":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_LEVEL"},{"presentationTable":"Time","presentationColumn":"Time Sequence","physicalTable":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_SEQ"},{"presentationTable":"Time","presentationColumn":"Time Sequence","physicalTable":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_SEQ"},{"presentationTable":"Time","presentationColumn":"Record Count","physicalTable":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_SEQ"},{"presentationTable":"Time","presentationColumn":"Record Count","physicalTable":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_SEQ"},{"presentationTable":"Time","presentationColumn":"Time Value","physicalTable":"SELECT DISTINCT     'FSCM' AS pillar,     '01' AS Calendar_Type,     'Year' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.fiscal_year_number DESC     ) AS time_seq,     TO_CHAR(b.fiscal_year_number) AS time_value FROM dw_fiscal_year_d a, dw_fiscal_year_d b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.fiscal_period_set_name        = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1           where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current') AND a.current_fiscal_year_code = 'Current'  AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Quarter' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_quarter AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_quarter_code,             fiscal_quarter_code   AS fiscal_quarter,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_quarter_number             ))AS run_count         FROM dw_fiscal_quarter_d     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_quarter_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT     'FSCM' AS pillar,      '01' AS Calendar_Type,     'Month' AS time_level,     SUM(1)OVER(         PARTITION BY b.fiscal_period_set_name         ORDER BY b.run_count DESC     ) AS time_seq,     b.fiscal_period_name AS time_value FROM     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             ))AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) a,     (         SELECT             fiscal_period_set_name,             fiscal_period_type,             current_fiscal_period_code,             fiscal_period_name,             (COUNT(1)OVER(                 PARTITION BY fiscal_period_set_name,fiscal_period_type                 ORDER BY                     fiscal_period_set_name,fiscal_period_type,fiscal_year_number,fiscal_period_number             )) AS run_count         FROM dw_fiscal_period_d         WHERE adjustment_period_flag = 'N'     ) b WHERE a.fiscal_period_set_name = b.fiscal_period_set_name  AND a.fiscal_period_type = b.fiscal_period_type  AND a.current_fiscal_period_code = 'Current'  AND a.fiscal_period_set_name       = (select min(a1.fiscal_period_set_name) from dw_fiscal_year_d a1          where a1.fiscal_year_number != -99999 and a1.current_fiscal_year_code = 'Current')  AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT DISTINCT     'HCM' AS pillar,     '02' AS Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_year_id DESC     ) AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM dw_year_d a, dw_year_d b WHERE a.current_cal_year_code = 'Current' AND b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_quarter_code DESC     ) AS time_seq,     b.cal_quarter_code AS time_value FROM      (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d     ) a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             )) AS run_count         FROM             dw_quarter_d              ) b  WHERE a.current_cal_qtr_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'HCM' AS pillar,     '02' AS Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY b.cal_month_start_date DESC     ) AS time_seq,     b.cal_month_code AS time_value FROM      (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d     ) a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM dw_month_d              ) b WHERE a.current_cal_month_code = 'Current' AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_VALUE"},{"presentationTable":"Time","presentationColumn":"Time Value","physicalTable":"select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Year' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.fiscal_year_number DESC)AS time_seq, TO_CHAR(b.fiscal_year_name) AS time_value from (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) a, (select distinct  fiscalcalendar fiscal_period_set_name, top_periodname fiscal_year_name,  to_number(to_char(top_startdate,'yyyy')) fiscal_year_number,  top_startdate year_start_date,  top_enddate year_end_date,  case when trunc(sysdate) between top_startdate and top_enddate then 'Current' else null end current_fiscal_year_code from DW_NS_FISCALCALPERIOD_DH ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_year_code = 'Current' AND b.fiscal_year_number BETWEEN a.fiscal_year_number - 19 AND a.fiscal_year_number UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Quarter' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_quarter_name) AS time_value from (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_quarter_name, current_fiscal_quarter_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,quarter_start_date,quarter_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level1_periodname fiscal_quarter_name,  level1_startdate quarter_start_date,  level1_enddate quarter_end_date,  case when trunc(sysdate) between level1_startdate and level1_enddate then 'Current' else null end current_fiscal_quarter_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_quarter_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION select 'NETSUITE' AS pillar, '01' as Calendar_Type, 'Month' AS time_level, SUM(1)OVER(PARTITION BY b.fiscal_period_set_name ORDER BY b.runcount DESC)AS time_seq, TO_CHAR(b.fiscal_month_name) AS time_value from (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) a, (select fiscal_period_set_name, fiscal_month_name, current_fiscal_month_code, COUNT(1)OVER(PARTITION BY fiscal_period_set_name ORDER BY fiscal_period_set_name,month_start_date,month_end_date) runcount from (select distinct  fiscalcalendar fiscal_period_set_name, level2_periodname fiscal_month_name,  level2_startdate month_start_date,  level2_enddate month_end_date,  case when trunc(sysdate) between level2_startdate and level2_enddate then 'Current' else null end current_fiscal_month_code from DW_NS_FISCALCALPERIOD_DH ) ) b  where a.fiscal_period_set_name=VALUEOF(NQ_SESSION.NETSUITE_SUBSIDIARY_CALENDAR) and a.fiscal_period_set_name = b.fiscal_period_set_name and a.current_fiscal_month_code = 'Current' AND b.runcount BETWEEN a.runcount - 19 AND a.runcount UNION SELECT DISTINCT     'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Year' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_year_id DESC     )AS time_seq,     TO_CHAR(b.cal_year_id)AS time_value FROM     dw_ns_year_d a,     dw_ns_year_d b WHERE      a.current_cal_year_code = 'Current'     AND     b.cal_year_id BETWEEN a.cal_year_id - 19 AND a.cal_year_id UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Quarter' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_quarter_code DESC     )AS time_seq,     b.cal_quarter_code AS time_value FROM       (         SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d     )a,     (SELECT             cal_quarter_code ,             current_cal_qtr_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_quarter_code             ))AS run_count         FROM             dw_ns_quarter_d              )b             WHERE a.current_cal_qtr_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count UNION SELECT       'NETSUITE' AS pillar,     '02' as Calendar_Type,     'Month' AS time_level,      ROW_NUMBER() OVER(         ORDER BY             b.cal_month_start_date DESC     )AS time_seq,     b.cal_month_code AS time_value FROM       (         SELECT             cal_month_start_date ,             current_cal_month_code,              cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d     )a,     (SELECT             cal_month_start_date ,             current_cal_month_code,             cal_month_code,             (COUNT(1)OVER(                     ORDER BY                     cal_year_id,cal_month_start_date             ))AS run_count         FROM             dw_ns_month_d              )b             WHERE a.current_cal_month_code = 'Current'     AND b.run_count BETWEEN a.run_count - 19 AND a.run_count","physicalColumn":"TIME_VALUE"}]}